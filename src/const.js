const FilterType = {
  ALL: 'all',
  FUTURE : 'future',
  PAST: 'past',
};

const SortType = {
  DEFAULT: 'default',
  PRICE: 'price',
  TIME: 'time',
};

const Mode = {
  DEFAULT: 'DEFAULT',
  EDITING: 'EDITING',
};

export {FilterType, SortType, Mode};
